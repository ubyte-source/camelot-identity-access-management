<?php

// START - ENG ==========================================================

namespace language\en\applications\sso\oauth\views\read\buttons {
    const delete = 'Delete';
    const cancel = 'Cancel';
    const loader = 'Operation in progress';
}

namespace language\en\applications\sso\oauth\views\read\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\sso\oauth\views\read\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}

namespace language\en\applications\sso\oauth\views\read\nav {
    const title = 'Search';
}

namespace language\en\applications\sso\oauth\views\read\nav\buttons {
    const add = 'add';
    const edit = 'edit';
    const delete = 'delete';
    const select = 'keep selection';
    const show_settings = 'show settings';
    const hide_settings = 'hide settings';
}

namespace language\en\applications\sso\oauth\views\read\infinite\search {
    const not_found = 'No result';
    const placeholder = 'Type to search';
}

namespace language\en\applications\sso\oauth\views\read\infinite {
    const no_result = 'No Result';
}

namespace language\en\applications\sso\oauth\views\read\infinite\settings {
    const input_placeholder = 'Type to search';
}

namespace language\en\applications\sso\oauth\views\read\sidepanel {
    const title = 'View Settings';
}

namespace language\en\applications\sso\oauth\views\read\modal\delete {
    const title = 'Are you sure you want to delete $0 oaut?';
    const notice = 'When a oauth is deleted, all the external users linked to the domains registered in this category won\'t be able to do the login.';
    const instructions = 'To continue with the deletion, enter the number of oauths you are about to delete. You must change users type from Oauth to Human beacuse the won\'t be able to do the login.';
    const error = 'The number of oauths does not match';
}

// END - ENG ==========================================================

// START - ITA ========================================================0

namespace language\it\applications\sso\oauth\views\read\buttons {
    const delete = 'Elimina';
    const cancel = 'Annulla';
    const loader = 'Operazione in corso';
}

namespace language\it\applications\sso\oauth\views\read\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\sso\oauth\views\read\nav {
    const title = 'Cerca';
}

namespace language\it\applications\sso\oauth\views\read\nav\buttons {
    const add = 'aggiungi';
    const edit = 'modifica';
    const delete = 'elimina';
    const select = 'copia selezione';
    const show_settings = 'mostra impostazioni';
    const hide_settings = 'nascondi impostazioni';
}
namespace language\it\applications\sso\oauth\views\read\infinite\search {
    const not_found = 'Nessun Risultato';
    const placeholder = 'Cerca';
}
namespace language\it\applications\sso\oauth\views\read\infinite {
    const no_result = 'Nessun risultato';
}

namespace language\it\applications\sso\oauth\views\read\infinite\settings {
    const input_placeholder = 'Cerca';
}

namespace language\it\applications\sso\oauth\views\read\sidepanel {
    const title = 'Impostazioni Vista';
}

namespace language\it\applications\sso\oauth\views\read\modal\delete {
    const title = 'Sicuro di voler eliminare $0 oauth?';
    const notice = 'Quando un oauth viene eliminato, gli utenti esterni associati ai domini contenuti in questa categoria non potranno più fare accesso al sistema.';
    const instructions = 'Per proseguire con l\'eliminazione, inserisci il numero di oauth che stai per eliminare. Ricordarsi di cambiare la tipologia di utente da Oauth a Umano.';
    const error = 'Il numero di oauth non corrisponde';
}

// END - ITA ==========================================================
