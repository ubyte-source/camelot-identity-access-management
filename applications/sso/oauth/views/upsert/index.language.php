<?php

// START - ENG ==========================================================

namespace language\en\applications\sso\oauth\views\upsert\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\sso\oauth\views\upsert\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}

namespace language\en\applications\sso\oauth\views\upsert\nav {
    const add = 'Add';
    const edit = 'Edit';
}

namespace language\en\applications\sso\oauth\views\upsert\buttons {
    const add = 'Add';
    const save = 'Save';
    const loader = 'Saving in progress';
}

namespace language\en\applications\sso\oauth\views\upsert\tooltip {
    const cidr = 'Enter the IP addresses in CIDR format (eg 100.100.100.100/24) that can access this application';
    const paste_icon = 'Click here to paste the elements';
}

namespace language\en\applications\sso\oauth\views\upsert\tabs {
    const remote = 'Remote parameters';
    const config = 'Configuration';
    const domain = 'Domains';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\oauth\views\upsert\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\sso\oauth\views\upsert\nav {
    const add = 'Crea';
    const edit = 'Modifica';
}

namespace language\it\applications\sso\oauth\views\upsert\buttons {
    const add = 'Crea';
    const save = 'Salva';
    const loader = 'Salvataggio in corso';
}

namespace language\it\applications\sso\oauth\views\upsert\tooltip {
    const cidr = 'Inserisci gli indirizzi IP in formato CIDR (es. 100.100.100.100/24) che possono accedere a questa applicazione';
    const paste_icon = 'Clicca qui per incollare gli elementi';
}

namespace language\it\applications\sso\oauth\views\upsert\tabs {
    const remote = 'Parametri remoti';
    const config = 'Configurazione';
    const domain = 'Domini';
}

// END - ITA ==========================================================
