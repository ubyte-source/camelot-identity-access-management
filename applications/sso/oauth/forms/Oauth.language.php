<?PHP

// START - ENG ========================================================

namespace language\en\applications\sso\oauth\forms\oauth\field {
    const name = 'Name';
    const client_id = 'Client ID';
    const client_secret = 'Client secret';
    const redirect_uri = 'Redirect URI';
    const tenant = 'Tenant';
    const authority = 'Authority';
    const token = 'Endpoint token';
    const authorize = 'Authorize';
    const scope = 'Scopes';
    const domain = 'Domains';
    const remote_whoami = 'Remote whoami';
}

namespace language\en\applications\sso\oauth\forms\required {
    const name = 'This field is necessary!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\oauth\forms\oauth\field {
    const name = 'Nome';
    const domain = 'Domini';
    const redirect_uri = 'URI di reindirizzamento';
    const token = 'Token endpoint';
    const authorize = 'Autorizzazione';
}

namespace language\it\applications\sso\oauth\forms\required {
    const name = 'Questo campo è obbligatorio!';
}

// END - ITA ==========================================================
