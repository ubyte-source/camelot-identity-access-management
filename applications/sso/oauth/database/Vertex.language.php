<?PHP

// START - ENG ==========================================================

namespace language\en\applications\sso\oauth\database\vertex {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\sso\oauth\database\vertex {
    const required = 'You don\'t have the policy to use this feature';
}

namespace language\en\applications\sso\oauth\database\forms\required {
    const name = 'This field is necessary!';
    const basename = 'This field is necessary!';
    const link = 'This field is necessary!';
    const _key = 'This field is necessary!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\oauth\database\vertex {
    const notice = 'I campi non sono compilati correttamente!';
}

namespace language\it\applications\sso\oauth\database\vertex {
    const required = 'Non hai i privilegi per eseguire queste operazione.';
}

namespace language\it\applications\sso\oauth\database\forms\required {
    const name = 'Questo campo è obbligatorio!';
    const basename = 'Questo campo è obbligatorio!';
    const link = 'Questo campo è obbligatorio!';
    const _key = 'Questo campo è obbligatorio!';
}

// END - ITA ==========================================================
