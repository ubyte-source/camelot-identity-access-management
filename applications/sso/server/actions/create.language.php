<?php

// START - ENG ========================================================

namespace language\en\applications\sso\server\actions {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\sso\server\actions\exception {
    const hash = 'Overlapping transitional, try again in a few moments!';
    const constrain = 'This server already exist!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\server\actions {
    const notice = 'I campi non sono compilati correttamente';
}

namespace language\it\applications\sso\server\actions\exception {
    const hash = 'Transizione sovrapposta, riprova tra qualche istante!';
    const constrain = 'Questo server esiste già!';
}

// END - ITA ==========================================================
