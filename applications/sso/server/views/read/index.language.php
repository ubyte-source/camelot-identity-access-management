<?php

// START - ENG ==========================================================

namespace language\en\applications\sso\server\views\read\buttons {
    const delete = 'Delete';
    const cancel = 'Cancel';
    const loader = 'Operation in progress';
}

namespace language\en\applications\sso\server\views\read\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\sso\server\views\read\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}

namespace language\en\applications\sso\server\views\read\nav {
    const title = 'Search';
}

namespace language\en\applications\sso\server\views\read\nav\buttons {
    const add = 'add';
    const edit = 'edit';
    const delete = 'delete';
    const select = 'keep selection';
    const show_settings = 'show settings';
    const hide_settings = 'hide settings';
}

namespace language\en\applications\sso\server\views\read\infinite\search {
    const not_found = 'No result';
    const placeholder = 'Type to search';
}

namespace language\en\applications\sso\server\views\read\infinite {
    const no_result = 'No Result';
}

namespace language\en\applications\sso\server\views\read\infinite\settings {
    const input_placeholder = 'Type to search';
}

namespace language\en\applications\sso\server\views\read\sidepanel {
    const title = 'View Settings';
}

namespace language\en\applications\sso\server\views\read\modal\delete {
    const title = 'Are you sure you want to delete $0 server?';
    const notice = 'When a server is deleted, all applications associated with it will be instantly excluded from IAM, terminating all active connections with the ecosystem. The associated whitelist will be permanently lost. The associated applications will continue to be available in the users menu but will not be usable until they are re-assigned to a valid server. This operation is final and cannot be canceled later.';
    const instructions = 'To continue with the deletion, enter the number of servers you are about to delete.';
    const error = 'The number of servers does not match';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\server\views\read\buttons {
    const delete = 'Elimina';
    const cancel = 'Annulla';
    const loader = 'Operazione in corso';
}

namespace language\it\applications\sso\server\views\read\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\sso\server\views\read\nav {
    const title = 'Cerca';
}

namespace language\it\applications\sso\server\views\read\nav\buttons {
    const add = 'aggiungi';
    const edit = 'modifica';
    const delete = 'elimina';
    const select = 'copia selezione';
    const show_settings = 'mostra impostazioni';
    const hide_settings = 'nascondi impostazioni';
}
namespace language\it\applications\sso\server\views\read\infinite\search {
    const not_found = 'Nessun Risultato';
    const placeholder = 'Cerca';
}
namespace language\it\applications\sso\server\views\read\infinite {
    const no_result = 'Nessun risultato';
}

namespace language\it\applications\sso\server\views\read\infinite\settings {
    const input_placeholder = 'Cerca';
}

namespace language\it\applications\sso\server\views\read\sidepanel {
    const title = 'Impostazioni Vista';
}

namespace language\it\applications\sso\server\views\read\modal\delete {
    const title = 'Sicuro di voler eliminare $0 server?';
    const notice = 'Quando un server viene eliminato, tutte le applicazioni associate ad esso verranno istantaneamente escluse da IAM, terminando tutte le connessioni attive con l\'ecosistema. La whitelist associata verrà persa definitivamente. Le applicazioni associate continueranno a rimanere a disposizione nel menù degli utenti ma non saranno utilizzabili fino a quando non saranno riassocaite ad un server valido. Questa operazione è definitiva e non può essere annullata in un secondo momento.';
    const instructions = 'Per proseguire con l\'eliminazione, inserisci il numero di server che stai per eliminare.';
    const error = 'Il numero di server non corrisponde';
}

// END - ITA ==========================================================
