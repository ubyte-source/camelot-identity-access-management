<?PHP

// START - ENG ==========================================================

namespace language\en\applications\sso\cluster\forms\cluster\field {
    const name = 'Name';
    const label = 'Display name';
    const language = 'Language';
    const description = 'Description';
}

namespace language\en\applications\sso\cluster\forms\cluster\field\language {
    const it = 'Italian';
    const en = 'English';
    const fr = 'French';
    const de = 'German';
    const es = 'Spanish';
}

namespace language\en\applications\sso\cluster\forms\vertex {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\sso\cluster\forms\vertex {
    const required = 'You don\'t have the policy to use this feature';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\cluster\forms\cluster\field {
    const name = 'Nome';
    const label = 'Nome visualizzato';
    const language = 'Lingua';
    const description = 'Descrizione';
}

namespace language\it\applications\sso\cluster\forms\cluster\field\language {
    const it = 'Italiano';
    const en = 'Inglese';
    const fr = 'Francese';
    const de = 'Tedesco';
    const es = 'Spagnolo';
}

namespace language\it\applications\sso\cluster\forms\vertex {
    const notice = 'I campi non sono compilati correttamente!';
}

namespace language\it\applications\sso\cluster\forms\vertex {
    const required = 'Non hai i privilegi per eseguire queste operazione.';
}

// END - ITA ==========================================================
