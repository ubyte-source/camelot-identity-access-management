<?php

// START - ENG ========================================================

namespace language\en\applications\sso\cluster\actions {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\sso\cluster\actions\exception {
    const hash = 'Overlapping transitional, try again in a few moments!';
    const share = 'The user "$0" is not in your hierarchy!';
    const constrain = 'This application already exist!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\cluster\actions {
    const notice = 'I campi non sono compilati correttamente';
}

namespace language\it\applications\sso\cluster\actions\exception {
    const hash = 'Transizione sovrapposta, riprova tra qualche istante!';
    const share = 'L\'utente "$0" non è nella tua gerarchia!';
    const constrain = 'Questa applicazione esiste già!';
}

// END - ITA ==========================================================
