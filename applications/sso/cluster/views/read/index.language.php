<?php

// START - ENG ==========================================================

namespace language\en\applications\sso\cluster\views\read\buttons {
    const delete = 'Delete';
    const cancel = 'Cancel';
    const loader = 'Operation in progress';
}

namespace language\en\applications\sso\cluster\views\read\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\sso\cluster\views\read\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}

namespace language\en\applications\sso\cluster\views\read\nav {
    const title = 'Search';
}

namespace language\en\applications\sso\cluster\views\read\nav\buttons {
    const add = 'add';
    const edit = 'edit';
    const delete = 'delete';
    const select = 'keep selection';
    const show_settings = 'show settings';
    const hide_settings = 'hide settings';
}

namespace language\en\applications\sso\cluster\views\read\infinite {
    const no_result = 'No result';
}

namespace language\en\applications\sso\cluster\views\read\infinite\settings {
    const input_placeholder = 'Type to search';
}

namespace language\en\applications\sso\cluster\views\read\sidepanel {
    const title = 'View Settings';
}
namespace language\en\applications\sso\cluster\views\read\infinite\search {
    const not_found = 'No Result';
    const placeholder = 'Type to search';
}

namespace language\en\applications\sso\cluster\views\read\modal\delete {
    const title = 'Are you sure you want to delete $0 cluster?';
    const notice = 'Before proceeding with the deletion of the selected clusters, make sure they do not have active connections with the applications. If a cluster still has connections with application entities, it will not be deleted. Clusters have only one organizational functionality in the IAM menu.';
    const instructions = 'To continue with the deletion, enter the number of clusters you are about to delete.';
    const error = 'The number of clusters does not match';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\cluster\views\read\buttons {
    const delete = 'Elimina';
    const cancel = 'Annulla';
    const loader = 'Operazione in corso';
}

namespace language\it\applications\sso\cluster\views\read\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\sso\cluster\views\read\nav {
    const title = 'Cerca';
}

namespace language\it\applications\sso\cluster\views\read\nav\buttons {
    const add = 'aggiungi';
    const edit = 'modifica';
    const delete = 'elimina';
    const select = 'copia selezione';
    const show_settings = 'mostra impostazioni';
    const hide_settings = 'nascondi impostazioni';
}

namespace language\it\applications\sso\cluster\views\read\infinite\search {
    const not_found = 'Nessun Risultato';
    const placeholder = 'Cerca';
}

namespace language\it\applications\sso\cluster\views\read\infinite {
    const no_result = 'Nessun risultato';
}

namespace language\it\applications\sso\cluster\views\read\infinite\settings {
    const input_placeholder = 'Cerca';
}

namespace language\it\applications\sso\cluster\views\read\sidepanel {
    const title = 'Impostazioni Vista';
}

namespace language\it\applications\sso\cluster\views\read\modal\delete {
    const title = 'Sicuro di voler eliminare $0 cluster?';
    const notice = 'Prima di procedere con l\'eliminazione dei cluster selezionati, assicurarsi che non abbiano connessioni attive con le applicazioni. Se un cluster ha ancora connessioni con le entità di applicazione questo non verrà eliminato. I cluster hanno solo una funzionalità organizzativa nel menù di IAM.';
    const instructions = 'Per proseguire con l\'eliminazione, inserisci il numero di cluster che stai per eliminare.';
    const error = 'Il numero di cluster non corrisponde';
}

// END - ITA ==========================================================
