<?php

// START - ENG ========================================================

namespace language\en\applications\sso\application\actions {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\sso\application\actions\exception {
    const hash = 'Overlapping transitional, try again in a few moments!';
    const constrain = 'This basename already exist!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\application\actions {
    const notice = 'I campi non sono compilati correttamente';
}

namespace language\it\applications\sso\application\actions\exception {
    const hash = 'Overlap transizionale, ritenta tra un paio di minuti';
    const constrain = 'Quest\'aplicazione è già stata creata';
}

// END - ITA ==========================================================
