<?php

// START - ENG ==========================================================

namespace language\en\applications\sso\application\views\read\buttons {
    const delete = 'Delete';
    const cancel = 'Cancel';
    const loader = 'Operation in progress';
}

namespace language\en\applications\sso\application\views\read\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\sso\application\views\read\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}
namespace language\en\applications\sso\application\views\read\infinite\search {
    const not_found = 'No Result';
    const placeholder = 'Type to search';
}

namespace language\en\applications\sso\application\views\read\nav {
    const title = 'Search';
}

namespace language\en\applications\sso\application\views\read\nav\buttons {
    const add = 'add';
    const edit = 'edit';
    const delete = 'delete';
    const select = 'keep selection';
    const show_settings = 'show settings';
    const hide_settings = 'hide settings';
}

namespace language\en\applications\sso\application\views\read\infinite {
    const no_result = 'No result';
}

namespace language\en\applications\sso\application\views\read\infinite\settings {
    const input_placeholder = 'Type to search';
}

namespace language\en\applications\sso\application\views\read\sidepanel {
    const title = 'View Settings';
}

namespace language\en\applications\sso\application\views\read\modal\delete {
    const title = 'Are you sure you want to delete $0 apps?';
    const notice = 'Before proceeding with the deletion of the selected applications, make sure that they have no active connections with the servers and that they have no active connections with the policies. If an application still has active external connections, it will not be deleted. All users and servers that access the application will be blocked. This will no longer be visible. This operation is final and cannot be canceled later.';
    const instructions = 'To continue with the deletion, enter the number of applications you are about to delete.';
    const error = 'The number of applications does not match';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\sso\application\views\read\buttons {
    const delete = 'Elimina';
    const cancel = 'Annulla';
    const loader = 'Operazione in corso';
}

namespace language\it\applications\sso\application\views\read\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\sso\application\views\read\nav {
    const title = 'Cerca';
}

namespace language\it\applications\sso\application\views\read\nav\buttons {
    const add = 'aggiungi';
    const edit = 'modifica';
    const delete = 'elimina';
    const select = 'copia selezione';
    const show_settings = 'mostra impostazioni';
    const hide_settings = 'nascondi impostazioni';
}
namespace language\it\applications\sso\application\views\read\infinite\search {
    const not_found = 'Nessun Risultato';
    const placeholder = 'Cerca';
}

namespace language\it\applications\sso\application\views\read\infinite {
    const no_result = 'Nessun risultato';
}

namespace language\it\applications\sso\application\views\read\infinite\settings {
    const input_placeholder = 'Cerca';
}

namespace language\it\applications\sso\application\views\read\sidepanel {
    const title = 'Impostazioni Vista';
}

namespace language\it\applications\sso\application\views\read\modal\delete {
    const title = 'Sicuro di voler eliminare $0 applicazioni?';
    const notice = 'Prima di procedere con l\'eliminazione delle applicazioni selezionate, assicurarsi che non abbiano connessioni attive con i server e che non abbiano connessioni attive con le policy. Se un\'applicazione ha ancora connessioni esterne attive questa non verrà eliminata. Tutti gli utenti ed i server che accedono all\'applicazione verranno bloccati. Questa non sarà più visibile. Questa operazione è definitiva e non può essere annullata in seguito.';
    const instructions = 'Per proseguire con l\'eliminazione, inserisci il numero di applicazioni che stai per eliminare.';
    const error = 'Il numero di applicazioni non corrisponde';
}

// END - ITA ==========================================================
