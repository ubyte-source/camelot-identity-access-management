<?php

// START - ENG ========================================================

namespace language\en\applications\iam\group\actions\exception {
    const admin = 'The user with the key "$0" not found';
    const group = 'The group with the key "$0" not found';
    const user = 'The user with the key "$0" not found';
    const loop = 'The group "$0" would form a loop. The request cannot be satisfied.';
}

namespace language\en\applications\iam\group\actions {
    const notice = 'The fields are not filled in correctly!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\iam\group\actions\exception {
    const admin = 'L\'utente con key "$0" non è stato trovato';
    const group = 'Il gruppo con key "$0" non è stato trovato';
    const user = 'L\'utente con key "$0" non è stato trovato';
    const loop = 'Il gruppo "$0" formerebbe un anello. La richiesta non può essere soddisfatta.';
}

namespace language\it\applications\iam\group\actions {
    const notice = 'I campi non sono compilati correttamente';
}

// END - ITA ==========================================================
