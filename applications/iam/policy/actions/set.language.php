<?php

// START - ENG ========================================================

namespace language\en\applications\iam\policy\actions\set {
    const required = 'The "$0" parameters are mandatory to complete the request.';
    const fields = 'Set the fields to be edit.';
    const policy = 'To complete this action you need to have the "$0" policy.';
    const errors = 'Fill in the fields correctly to complete the request.';
}

namespace language\en\applications\iam\policy\actions\exception {
    const manager = 'You are not the manager of this application!';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\iam\policy\actions\set {
    const required = 'I parametri "$0" sono obbligatori per completare la richiesta.';
    const fields = 'Imposta i campi da modificare.';
    const policy = 'Per eseguire questa azione devi avare la "$0" policy';
    const errors = 'Riempi i campi correttamente per completare la richiesta.';
}

namespace language\it\applications\iam\policy\actions\exception {
    const manager = 'Non sei l\'admin di quest\'applicazione';
}

// END - ITA ==========================================================
