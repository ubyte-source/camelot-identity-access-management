<?PHP

// START - ENG ==========================================================

namespace language\en\applications\iam\policy\forms\upsert\field {
    const _key = 'Key';
    const _rev = 'Rev';
    const application = 'Application';
    const name = 'Name';
    const route = 'Route';
    const description = 'Description';
}

namespace language\en\applications\iam\policy\forms\vertex {
    const notice = 'The fields are not filled in correctly!';
}

namespace language\en\applications\iam\policy\forms\vertex {
    const required = 'You don\'t have the policy to use this feature';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\iam\policy\forms\upsert\field {
    const _key = 'Key';
    const _rev = 'Rev';
    const application = 'Applicazione';
    const name = 'Nome';
    const route = 'Route';
    const description = 'Descrizione';
}

namespace language\it\applications\iam\policy\forms\vertex {
    const notice = 'I campi non sono compilati correttamente!';
}

namespace language\it\applications\iam\policy\forms\vertex {
    const required = 'Non hai i privilegi per eseguire queste operazione.';
}

// END - ITA ==========================================================
