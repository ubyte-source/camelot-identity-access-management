<?php

// START - ENG ==========================================================

namespace language\en\applications\iam\policy\views\read\buttons {
    const delete = 'Delete';
    const cancel = 'Cancel';
    const loader = 'Operation in progress';
}

namespace language\en\applications\iam\policy\views\read\header {
    const app_name = 'Identity & Access Platform';
}

namespace language\en\applications\iam\policy\views\read\header\buttons {
    const my_account = 'My Account';
    const logout = 'Logout';
}

namespace language\en\applications\iam\policy\views\read\nav {
    const title = 'Search';
}

namespace language\en\applications\iam\policy\views\read\nav\buttons {
    const add = 'add';
    const edit = 'edit';
    const delete = 'delete';
    const select = 'keep selection';
    const show_settings = 'show settings';
    const hide_settings = 'hide settings';
}

namespace language\en\applications\iam\policy\views\read\infinite {
    const no_result = 'No result';
}

namespace language\en\applications\iam\policy\views\read\infinite\setting {
    const placeholder = 'Type to search';
}

namespace language\en\applications\iam\policy\views\read\sidepanel {
    const title = 'View Settings';
}

namespace language\en\applications\iam\policy\views\read\modal\delete {
    const title = 'Are you sure you want to delete $0 policy?';
    const notice = 'If you proceed to delete these policies, all applications will immediately stop the associated functions. All associated users will lose control of these features. Policies will also be automatically removed from groups. This operation is final and cannot be canceled later.';
    const instructions = 'To continue with the deletion, enter the number of policies you are about to delete.';
    const error = 'The number of policy does not match';
}

// END - ENG ==========================================================

// START - ITA ========================================================

namespace language\it\applications\iam\policy\views\read\buttons {
    const delete = 'Elimina';
    const cancel = 'Annulla';
    const loader = 'Operazione in corso';
}

namespace language\it\applications\iam\policy\views\read\header\buttons {
    const my_account = 'Profilo';
    const logout = 'Esci';
}

namespace language\it\applications\iam\policy\views\read\nav {
    const title = 'Cerca';
}

namespace language\it\applications\iam\policy\views\read\nav\buttons {
    const add = 'aggiungi';
    const edit = 'modifica';
    const delete = 'elimina';
    const select = 'copia selezione';
    const show_settings = 'mostra impostazioni';
    const hide_settings = 'nascondi impostazioni';
}

namespace language\it\applications\iam\policy\views\read\infinite {
    const no_result = 'Nessun risultato';
}

namespace language\it\applications\iam\policy\views\read\infinite\setting {
    const placeholder = 'Cerca';
}

namespace language\it\applications\iam\policy\views\read\sidepanel {
    const title = 'Impostazioni Vista';
}

namespace language\it\applications\iam\policy\views\read\modal\delete {
    const title = 'Sicuro di voler eliminare $0 policy?';
    const notice = 'Se si procede all\'eliminazione di queste policy, tutte le applicazioni interromperanno immediatamente le funzioni associate. Tutti gli utenti associati perderanno il controllo di queste funzionalità. Le policy verranno inoltre rimosse automaticamente anche dai gruppi. Questa operazione è definitiva e non può essere annullata in seguito.';
    const instructions = 'Per proseguire con l\'eliminazione, inserisci il numero di policy che stai per eliminare.';
    const error = 'Il numero di policy non corrisponde';
}

// END - ITA ==========================================================
